[build]
builder = "NIXPACKS"
buildCommand = "cd apps/bff && pnpm install && pnpm run build"

[deploy]
startCommand = "node apps/bff/dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
